<!-- Multiply satoshi by the market price and divide it by 100,000,000 -->
<% if @show_data && @show_data != "" %> 
<% address = @rbody["address"] %>
<% thePriceTotal = @rbody["total_received"] * @parsed_response["market_price_usd"] / 100000000 %>
<% @IOlabel = "UNKNOWN" %>
<% end %>
<h1>Block Chain Stats</h1><br />


<div class="row"> <!-- Start of first row -->
  <div class="col-md-1"></div>
  <div class="col-md-5">  <!-- Start of col-md-6 -->
    <!-- Only show the results of the api call if show_data exists and is not empty -->
    <% if @show_data && @show_data != "" %> 
    <table class="table table-striped">
      <tr>
        <td colspan="2"><strong>Summary</strong></td>
      </tr>
      <tr>
        <td>Hash 160</td>
        <td><%=@rbody["hash160"] %></td>
      </tr>
      <tr>
        <td>Address</td>
        <td><%=@rbody["address"] %></td>
      </tr>
    </table>
    <% end %>
    <%= @customError %>
  </div>
  <div class="col-md-5">
   <!-- Only show the results of the api call if show_data exists and is not empty -->
   <% if @show_data && @show_data != "" %> 
   <table class="table table-striped">
    <tr>
      <td colspan="2"><strong>Transactions</strong></td>
    </tr>
    <tr>
      <td>Number of Transactions</td>
      <td><%=@rbody["n_tx"] %></td>
    </tr>
    <tr>
      <td>Total Received</td>
      <td>
       <%=number_to_currency(thePriceTotal) %>
     </td>
   </tr>
   <tr>
    <td>Final Balance</td>
    <td><%=@rbody["final_balance"] %></td>
  </tr>
</table>

<% end %>
</div><!-- Let's end col-md-6 -->
<div class="col-md-1"></div>
</div> <!-- Let's end row here -->

<!-- We are starting a new row here -->
<div class="row">
  <div class="col-md-1"></div>
  <div class="col-md-10">
    <table class="table table-striped">
      <tr>
        <td colspan="3">
          <strong>Transactions</strong>
        </td>
      </tr>

      <% if @show_data && @show_data != "" %> 

      <% @trans.each do |one_trans| %>

      <tr>
        <td>
          <br /> TRANSACTION
          <br /> Hash: <%=one_trans["hash"] %>
        </td>
        <td></td>
      </tr>
      <tr>
        <td>
         <br /> Senders<br />
         <% @IOlabel = "" %>
         <% one_trans["inputs"].each do |one_input| %>
          <% if one_input["prev_out"]["addr"] == @rbody["address"] %>
          <% @IOlabel = "SENDER" %>
          <strong> 
          <% end %>
         <i> Sender:</i> <%= one_input["prev_out"]["addr"] %>- <%= number_to_currency(one_input["prev_out"]["value"] * @parsed_response["market_price_usd"] / 100000000) %><br />
         
          <% if one_input["prev_out"]["addr"] == @rbody["address"] %>
          </strong> 
          <% end %>
         <% end %>
         <br />
         <%=  @IOlabel %>
       </td>

       <td>

        
         <br />---Transaction Receivers---<br />
         <% @IOlabel = "" %>
         <% one_trans["out"].each do |one_out| %>
          <% if one_out["addr"] == @rbody["address"] %>
            <% @IOlabel = "RECEIVER" %>
          <strong> 
          <% end %>
         <i>Receiver:</i> <%= one_out["addr"] %> - <%= number_to_currency(one_out["value"] * @parsed_response["market_price_usd"] / 100000000) %><br />
          <% if one_out["addr"] == @rbody["address"] %>
          </strong> 
          <% end %>
         <% end %>
         <br />
         <%=  @IOlabel %>
       </td>
     </tr>
     <% end %>
     <% end %>




   </table>
 </div>
 <div class="col-md-1"></div>
</div>







<br /><br /><br />

  </div>

